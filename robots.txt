User-agent: *
Sitemap: https://dorxmoon.github.io/business-card/sitemap.xml

# Запрещать к индексации нечего: редактор портфолио живёт ТОЛЬКО локально: «ЗАПУСТИТЬ ЛОКАЛЬНО.cmd» →
# http://localhost:8080/admin.html. На хостинг он не выкладывается вовсе,
# Строка Disallow отсюда убрана сознательно: она ничего не закрывала (файла
# на хостинге нет), зато называла посторонним адрес админской страницы —
# robots.txt читают все, включая тех, кто ищет, куда постучаться.
#
# Схема «страница-замок + зашифрованный редактор на проде» (studio.html,
# studio.data, tools/lock-admin.js) осталась на диске, но с сайта снята:
# на статике всё равно нужен коммит и пуш, чтобы правки доехали до прода,
# а значит веб-доступ к редактору не давал ничего, кроме лишней двери.
